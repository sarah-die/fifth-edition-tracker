info:
  name: gameStates
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/gameStates"
  body:
    type: json
    data: '{"state": {"characters": []}}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
